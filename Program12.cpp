#include <iostream>
#include <string>

using namespace std;

void program12() {
    // Input
    const float PI = 3.1417;
    double R = 3.0, H = 4.0;

    // Processing
    double Volume = PI * (R * R) * H;

    // Output
    cout << "Volume of cylinder: " << Volume << endl;
    // return 0;
}
